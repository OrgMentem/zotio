{
  "schema_version": 1,
  "applied_at": "2026-05-11",
  "base_run_id": "20260511-103605",
  "base_printing_press_version": "4.2.2",
  "patches": [
    {
      "id": "zotero-item-workflows",
      "summary": "Added hand-written Zotero item workflow commands under internal/cli.",
      "reason": "The generated CLI omitted annotation, full-text, recent-item, duplicate, identifier lookup, and collection move workflows requested for this local tree.",
      "files": [
        "internal/cli/items.go",
        "internal/cli/items_annotations.go",
        "internal/cli/items_fulltext.go",
        "internal/cli/items_recent.go",
        "internal/cli/items_duplicates.go",
        "internal/cli/items_find.go",
        "internal/cli/items_move.go",
        "internal/cli/items_json_helpers.go"
      ],
      "validated_outcome": "go build ./... and go vet ./... pass; items help lists the new commands."
    },
    {
      "id": "zotero-import-tag-collection-restore-workflows",
      "summary": "Added hand-written Zotero tag rename, import, collection move, and item restore commands.",
      "reason": "The generated CLI omitted these Zotero-specific mutation workflows requested for this local tree.",
      "files": [
        "internal/cli/tags.go",
        "internal/cli/tags_rename.go",
        "internal/cli/import.go",
        "internal/cli/import_doi.go",
        "internal/cli/import_url.go",
        "internal/cli/import_file.go",
        "internal/cli/collections.go",
        "internal/cli/collections_move.go",
        "internal/cli/items.go",
        "internal/cli/items_restore.go"
      ],
      "validated_outcome": "go fmt ./..., go build ./..., and go vet ./... pass; help output lists the new commands."
    },
    {
      "id": "zotero-local-library-audits",
      "summary": "Added hand-written local tag, item, and library audit reports.",
      "reason": "The generated CLI advertised these Zotero-specific local analytics workflows but did not include implementations in the generated command tree.",
      "files": [
        "internal/cli/root.go",
        "internal/cli/items.go",
        "internal/cli/items_audit.go",
        "internal/cli/items_missing_pdf.go",
        "internal/cli/items_unfiled.go",
        "internal/cli/tags.go",
        "internal/cli/tags_audit.go",
        "internal/cli/library.go",
        "internal/cli/library_stats.go"
      ],
      "validated_outcome": "go build ./..., go vet ./..., and go fmt ./... pass."
    },
    {
      "id": "zotero-expanded-item-reports",
      "summary": "Added hand-written Zotero item reports for citation keys, authors, venues, stale items, note templates, desktop opens, and collection membership.",
      "reason": "The generated CLI advertised these Zotero-specific reading and maintenance workflows but did not include implementations in the generated command tree.",
      "files": [
        "internal/cli/items.go",
        "internal/cli/items_citekey_conflicts.go",
        "internal/cli/items_authors.go",
        "internal/cli/items_venues.go",
        "internal/cli/items_stale.go",
        "internal/cli/items_open.go",
        "internal/cli/items_note_template.go",
        "internal/cli/items_collections_of.go",
        "internal/cli/helpers.go"
      ],
      "validated_outcome": "go fmt ./..., go build ./..., and go vet ./... pass."
    },
    {
      "id": "zotero-annotation-reading-search-preprint-workflows",
      "summary": "Added hand-written annotation, tag inventory, reading queue, saved search, and preprint check commands.",
      "reason": "The generated CLI advertised these Zotero-specific research workflows but did not include complete command implementations in the generated command tree.",
      "files": [
        "internal/cli/root.go",
        "internal/cli/annotations.go",
        "internal/cli/annotations_export.go",
        "internal/cli/annotations_timeline.go",
        "internal/cli/annotations_search.go",
        "internal/cli/tags.go",
        "internal/cli/tags_inventory.go",
        "internal/cli/reading_list.go",
        "internal/cli/searches.go",
        "internal/cli/searches_run.go",
        "internal/cli/items.go",
        "internal/cli/items_preprint_check.go",
        "internal/cli/helpers.go"
      ],
      "validated_outcome": "go build ./..., go vet ./..., and go fmt ./... pass."
    },
    {
      "id": "zotero-glean-finding-fixes",
      "summary": "Fixed correctness/security defects surfaced by glean review missions: AdaptiveLimiter.Wait pacing race, `items get` provenance always counting 0, ResolveByName json_extract field-name injection guard, maskToken short-token exposure, dead OAuth token-refresh path (Zotero is API-key auth), and concurrency-aware sync progress (\\r garble under parallel workers).",
      "reason": "glean static-audit/security-audit missions found real bugs in generated infrastructure files. Fixed in-repo per the local patch protocol. The Wait race, provenance count, and maskToken are SYSTEMIC (affect every printed CLI) and should be pushed upstream to cli-printing-press; the token-refresh removal is Zotero-specific (no OAuth endpoint).",
      "files": [
        "internal/cliutil/ratelimit.go",
        "internal/cli/items_get.go",
        "internal/store/store.go",
        "internal/client/client.go",
        "internal/cli/sync.go"
      ],
      "validated_outcome": "go build/vet pass; new regression tests TestAdaptiveLimiterWaitConcurrentPacing (also under -race), TestCountResultItems, TestIsSafeJSONFieldName, TestMaskToken pass. Verified false positives left unchanged: SQL multi-statement read-only bypass (empirically: db.Query runs one statement and mode=ro blocks writes) and global cache invalidation (documented opaque-key design)."
    },
    {
      "id": "zotero-glean-test-gap-coverage",
      "summary": "Added unit-test coverage for five previously-untested generated modules flagged by glean test-gaps findings: HTTP client (do/retry/cache/sanitize/cacheKey), sync (pagination/cursor-stuck/maxPages/single-object/access-warning + pure helpers), CLI helpers (error classification/filtering/formatting/pagination), search (extractSearchResults shapes + live-filter + local fallback), and doctor (interstitial detection/fail-on policy/cache report).",
      "reason": "glean test-gaps missions (beads pg9a, rf41, bnt5, f6yb, hcaq) flagged P0/P1 untested logic. Tests use real httptest servers, real store.Open temp DBs, and temp dirs (no mocks) and exercise real branch behavior.",
      "files": [
        "internal/client/client_gaps_test.go",
        "internal/cli/sync_gaps_test.go",
        "internal/cli/helpers_gaps_test.go",
        "internal/cli/search_gaps_test.go",
        "internal/cli/doctor_gaps_test.go"
      ],
      "validated_outcome": "32 new test functions; go build/vet and full `go test ./...` pass (fresh, -count=1)."
    },
    {
      "id": "zotero-aca7-version-based-sync",
      "summary": "Reworked incremental sync to Zotero's integer library-version model. Added a library_version column to sync_state with SaveLibraryVersion/GetLibraryVersion; surfaced the Last-Modified-Version response header via client.GetWithVersion (do() refactored to doRequest returning http.Header); syncResource now reads/sends an integer `since` and stores the checkpoint; `--since` changed from a duration string to an integer library version (matching `items list --since`); removed the dead parseSinceDuration/RFC3339 path.",
      "reason": "glean bead aca7 (P1): sync converted --since and last_synced_at to RFC3339 timestamps and passed them to Zotero's `since` param, which expects an integer library version, so incremental sync silently never filtered. Required response-header plumbing the client did not previously expose.",
      "files": [
        "internal/store/store.go",
        "internal/client/client.go",
        "internal/cli/sync.go",
        "internal/cli/sync_gaps_test.go",
        "internal/client/getversion_patch_test.go"
      ],
      "validated_outcome": "TestSyncResourceVersionBasedIncremental (stores Last-Modified-Version, sends since=<stored> on the next run, explicit --since overrides) and TestGetWithVersion pass; go build/vet and full `go test ./...` pass (fresh)."
    },
    {
      "id": "zotero-glean-p3-cleanups",
      "summary": "P3 cleanups from glean: (e5o8) parallelize annotations export child-fetches via cliutil.FanoutRun instead of a sequential Get-per-item loop, preserving source order and fail-fast error semantics; (zog1) extract the duplicated HTTP-status detection ladder into cliutil.ClassifyHTTPError used by both the CLI classifyAPIError and the MCP error handler, keeping each layer's hint text and result type; (3ta0) add an overridable retryAfterNow clock seam so the RetryAfter HTTP-date/epoch tests assert exact durations instead of a loose 5-8s range.",
      "reason": "glean beads e5o8 (perf), zog1 (maintainability: brittle strings.Contains(msg,\"HTTP NNN\") ladder copy-pasted across internal/cli and internal/mcp), 3ta0 (test precision on wall-clock-dependent parsing).",
      "files": [
        "internal/cli/annotations_export.go",
        "internal/cliutil/text.go",
        "internal/cli/helpers.go",
        "internal/mcp/tools.go",
        "internal/cliutil/ratelimit.go",
        "internal/cliutil/cliutil_test.go",
        "internal/cliutil/classify_patch_test.go"
      ],
      "validated_outcome": "TestClassifyHTTPError + exact RetryAfter time tests pass; CLI/MCP error messages unchanged (only detection deduplicated); go build/vet and full `go test ./...` pass (fresh)."
    },
    {
      "id": "zotero-glean-a91x-tail-change-feed",
      "summary": "Reworked `tail` from a full-snapshot re-fetch into a deduplicated version-cursor change feed: per-resource cursor persisted in sync_state under tail:<resource>, ?since= delta fetch, upsert/delete NDJSON events, /deleted handling, per-cycle delivery to webhook/file sinks, and --follow=false single-poll support. Restricted tail resources to the four with a change feed (items, collections, searches, tags).",
      "reason": "glean bead a91x (P1): tail re-emitted every object every poll with no dedup or version tracking, so consumers could not tell what actually changed.",
      "files": [
        "internal/cli/tail.go",
        "internal/cli/tail_gaps_test.go"
      ],
      "validated_outcome": "TestEmitChanges_ChangeFeed and TestEmitChanges_WebhookDelivery pass: baseline emits upserts + records cursor; delta emits one upsert and one delete and advances the cursor; webhook sink receives the cycle NDJSON."
    },
    {
      "id": "zotero-glean-hhup-dependent-resource-sync",
      "summary": "Sync annotations and PDF full text as first-class dependent resources: indexed parent_key/item_type/annotation_color/item_date columns on resources (populated from each item's nested data sub-object), idx_resources_parent_key/idx_resources_item_type, store helpers ItemsByType/AnnotationsForItem/Fulltext, an opt-in `sync --fulltext` pass (always sends since, fans out attachment fetches, stores fulltext rows FTS-indexed for search), and local-first reworks of items fulltext / annotations search / export / timeline with a --refresh live override.",
      "reason": "glean bead hhup (P1): annotations/attachments were stored only as opaque JSON with no indexed parent/type columns and no full text, so every annotation/fulltext consumer fetched live (N+1).",
      "files": [
        "internal/store/store.go",
        "internal/store/store_hhup_test.go",
        "internal/cli/sync.go",
        "internal/cli/sync_hhup_test.go",
        "internal/cli/items_fulltext.go",
        "internal/cli/annotations_search.go",
        "internal/cli/annotations_export.go",
        "internal/cli/annotations_timeline.go"
      ],
      "validated_outcome": "store_hhup_test (indexed columns populated; ItemsByType + AnnotationsForItem join annotation->attachment->top item; Fulltext round-trip) and sync_hhup_test (syncFulltext stores ATT1, db.Search finds it, fulltext cursor advances) pass; full `go test ./...` green."
    },
    {
      "id": "zotero-glean-c4ke-conditional-auth-inprocess-mcp",
      "summary": "Made ZOTERO_API_KEY conditional in the remaining metadata (tools-manifest.json env_var_specs required=false + description, handleContext auth block required=false + description, README env-var table and `claude mcp add` note) and replaced the MCP command-mirror shell-out with in-process Cobra execution (cobratree.RegisterAll takes a *cobra.Command factory; inProcessHandler runs a fresh tree per call capturing OutOrStdout; deleted cli_path.go). Excluded the streaming `tail` command from the in-process mirror.",
      "reason": "glean bead c4ke (P1): metadata hard-required an API key the local desktop transport never needs, and command-mirror tools shelled out to a companion zotero-pp-cli binary that fails when not on PATH.",
      "files": [
        "tools-manifest.json",
        "internal/mcp/tools.go",
        "internal/mcp/cobratree/walker.go",
        "internal/mcp/cobratree/shellout.go",
        "internal/mcp/cobratree/classify.go",
        "internal/mcp/cobratree/inprocess_test.go",
        "README.md"
      ],
      "validated_outcome": "cli_path.go removed (no SiblingCLIPath references remain); TestInProcessHandler_Success/_Error and RegisterAll smoke tests pass; go build ./... and full `go test ./...` green."
    },
    {
      "id": "zotero-glean-9bfn-group-libraries",
      "summary": "Added group-library support via per-library DB files: a --group <id> root flag (validated numeric) sets activeGroupID, which routes defaultDBPath to data-group-<id>.db and rewrites the API library prefix to /groups/<id> in newClient; a new `groups list` discovery command enumerates the user's groups; doctor reports the active library. Personal library is unchanged (data.db, /users/<id>).",
      "reason": "glean bead 9bfn (P1): library scope lived entirely in config.BaseURL with no group discovery, selector, or storage isolation.",
      "files": [
        "internal/cli/root.go",
        "internal/cli/helpers.go",
        "internal/cli/groups.go",
        "internal/cli/groups_test.go",
        "internal/cli/doctor.go"
      ],
      "validated_outcome": "groups_test (rewriteLibraryPrefix for user/web/existing-group/no-segment cases; userIDFromBaseURL extraction + group-URL rejection; group-aware defaultDBPath; groups list JSON + table output; group base URL rejected) passes; full `go test ./...` green."
    },
    {
      "id": "zotero-glean-cvl6-local-query-planner",
      "summary": "Added a Zotero-aware local query planner so --data-source local replays endpoint scoping instead of dumping all synced rows. New store query layer (internal/store/query.go): QueryItems applies itemType (indexed column), tag and collection membership (json_each), top-level filter, quick-search (FTS), sort/direction (json_extract), and limit/start; buildSearchDocument indexes a curated item document (key, bibliographic fields, creators, tags, annotation text) into resources_fts instead of the raw JSON blob; SearchByType scopes FTS to one resource type. resolveLocal routes item-list paths (/items, /items/top, /collections/{key}/items[/top]) through the planner and advertises meta.scoped; the search command's previously no-op default local branch now runs cross-resource FTS.",
      "reason": "glean bead cvl6 (P1): the local store was a generic JSON cache with FTS bolted on — resolveLocal ignored endpoint filters/path scoping, the search command's default local branch was a placeholder that returned nothing, and FTS indexed raw JSON rather than Zotero search fields, so the headline offline mode could not match live reads.",
      "files": [
        "internal/store/query.go",
        "internal/store/query_test.go",
        "internal/store/store.go",
        "internal/cli/data_source.go",
        "internal/cli/data_source_cvl6_test.go",
        "internal/cli/search.go",
        "internal/cli/helpers.go",
        "README.md"
      ],
      "validated_outcome": "query_test (itemType/tag/collection/top/limit/start/sort + quick-search + curated FTS doc) and fixture-backed parity tests (live httptest vs --data-source local return identical key sets and ordering for itemType+sort and tag scopes; empty scope returns [] not an error) pass; gofmt/build/vet and full `go test ./...` green. Scope note: implemented as a hand-written Zotero item read model (the high-value list/get/search scopes), not a fully generic per-endpoint translator."
    },
    {
      "id": "zotero-glean-qfuq-mcp-resources-prompts",
      "summary": "Added first-class MCP resources and prompts so the server is no longer tool-only. Resources: zotero://context (durable domain taxonomy, shared with the context tool via domainContext), zotero://agent-context (CLI description via new cli.AgentContextJSON), zotero://status (local archive sync state), zotero://schema (local SQLite DDL), and templates zotero://collections/{key} (collection manifest) and zotero://items/{key} (item + annotations bundle). Prompts: inspect-library, export-reading-notes, prepare-citation-export. main.go advertises resource+prompt capabilities and calls RegisterResources/RegisterPrompts; mutations stay typed tools.",
      "reason": "glean bead qfuq (P2): the MCP surface mirrored CLI commands only — no resource/prompt capabilities — so hosts had to shell through mirrored tools and guess command sequences to discover library state and workflows.",
      "files": [
        "cmd/zotero-pp-mcp/main.go",
        "internal/mcp/resources.go",
        "internal/mcp/resources_test.go",
        "internal/mcp/tools.go",
        "internal/cli/mcp_exports.go",
        "README.md"
      ],
      "validated_outcome": "JSON-RPC tests through server.HandleMessage (as an MCP inspector would) confirm resources/list, resources/templates/list, and prompts/list return the new entries; resources/read zotero://context matches the context tool payload, zotero://agent-context carries schema_version+commands, zotero://status reports synced:false on an empty store; seeded-store tests confirm the collection manifest (item_count + member keys) and item bundle (annotation_count via attachment join) templates. gofmt/build/vet and full `go test ./...` green."
    },
    {
      "id": "zotero-glean-dk33-metadata-enrichment",
      "summary": "Added `items enrich`, a provider-backed remediation pipeline that turns the read-only `items audit` work queues into fixes. --missing-doi resolves a DOI from CrossRef (exact normalized-title match to avoid wrong attaches), --missing-abstract fills the abstract from CrossRef by the item's DOI (JATS markup stripped), and --missing-pdf attaches an open-access PDF link from Unpaywall. It previews a patch plan by default and applies via the existing PATCH/POST paths only with --yes (or --agent), including the item version as a conflict guard and appending enrichment provenance to the item's Extra field. Provider base URLs are overridable for testing; apply is unit-testable via a small apiMutator interface.",
      "reason": "glean bead dk33 (P2): the CLI had strong audit/reporting (items audit: missing_pdf/abstract/doi/tags) but no path from a detected defect to a safe, provider-backed fix — users had to export TODO lists into external scripts.",
      "files": [
        "internal/cli/items_enrich.go",
        "internal/cli/items_enrich_test.go",
        "internal/cli/items.go",
        "internal/cli/import_doi.go",
        "README.md"
      ],
      "validated_outcome": "Tests: CrossRef DOI exact-title match (and rejection of non-matching candidates), CrossRef abstract JATS stripping, Unpaywall OA-PDF resolution (and no-OA case), apply step builds a PATCH body with version+field+Extra provenance and a POST linked_url attachment (via apiMutator), store-backed work queue produces a proposal for the matching item and skips the non-matching one, dry-run preview reports unapplied proposals, and an end-to-end apply through the real client to an httptest Zotero server PATCHes DOI+version. gofmt/build/vet and full `go test ./...` green."
    },
    {
      "id": "zotero-glean-37jv-import-translators",
      "summary": "Metadata-enriched URL/DOI imports. `import url` previously created a bare webpage item whose title was the URL; it now runs a translator pipeline (internal/cli/import_translate.go): a DOI embedded in the URL resolves full metadata from CrossRef, otherwise the page's embedded citation_*/Open Graph/Dublin Core meta tags (the signals Zotero's Embedded Metadata translator reads) are mapped into a typed item (title, creators, DOI, date, publication venue, abstract, itemType), with a bare webpage item as the last-resort fallback. The CrossRef item mapper now also captures the abstract (JATS stripped), so `import doi` returns richer items. Both `import url` and `import doi` gained a --dry-run preview of the request body and preserve --collection. The CrossRef base URL is an overridable test seam.",
      "reason": "glean bead 37jv (P2): import url/doi bypassed richer metadata and created sparse items (webpage with title == URL; DOI mapping limited to type/title/creators/year/DOI/publicationTitle), so one of the highest-frequency Zotero workflows produced references needing manual cleanup.",
      "files": [
        "internal/cli/import_translate.go",
        "internal/cli/import_translate_test.go",
        "internal/cli/import_url.go",
        "internal/cli/import_doi.go"
      ],
      "validated_outcome": "Tests: DOI extraction from URLs (article/PDF/none), HTML meta parsing (repeated citation_author) + typed item mapping (title/creators/DOI/journal/abstract/itemType), nil when no usable title, buildImportItemFromURL across DOI-in-URL (CrossRef, URL preserved, abstract), embedded-metadata, and non-HTML fallback paths, plus command-level --dry-run previews for url (collection preserved) and doi (abstract included) that write nothing. gofmt/build/vet and full `go test ./...` green. Scope: provider (CrossRef) + embedded-metadata translator; the live Zotero desktop-connector translator and OpenAlex remain future sources behind the same pipeline."
    },
    {
      "id": "zotero-glean-49r4-vault-sync",
      "summary": "Added `vault sync`, a vault-aware Obsidian/Logseq note materializer (internal/cli/vault_sync.go). One Markdown note per regular item, filename keyed on the native/BBT citation key (data.citationKey, falling back to extra then the item key), scoped from the local store via the cvl6 query planner (--collection/--tag/--item-type/--limit, top-level items only). Notes carry zotero:// backlinks (item select in frontmatter; per-annotation open-pdf with page label, group-aware via activeGroupID) and embed current annotations (hhup AnnotationsForItem join) in a fenced managed block. Re-runs are idempotent and non-destructive: only managed frontmatter keys and the fenced block change; user prose and other frontmatter keys are preserved. --dry-run previews created/updated/unchanged. Also wrote docs/obsidian-integration.md positioning the CLI vs the Obsidian/Zotero plugin ecosystem.",
      "reason": "glean bead 49r4 (P2): note output was stdout-only template scaffolding (items note-template) with an annotations placeholder, and the only delivery sinks were stdout/file/webhook — no stateful, non-clobbering vault integration for the documented Obsidian/Logseq workflow.",
      "files": [
        "internal/cli/vault_sync.go",
        "internal/cli/vault_sync_test.go",
        "internal/cli/root.go",
        "docs/obsidian-integration.md"
      ],
      "validated_outcome": "Tests: Obsidian note creation (citekey filename, frontmatter, zotero://select + per-annotation open-pdf backlinks, page label, comment), idempotent re-run (unchanged, byte-identical), dry-run writes nothing, the critical no-clobber merge (managed keys updated while user frontmatter keys + prose survive), managed-block replacement without duplication, Logseq creation, filename sanitization, and --out required. Verified the native local API on Zotero 10.0-beta.7 returns full item data incl. data.citationKey. gofmt/build/vet and full `go test ./...` green. Scope: Obsidian merge is the fully-featured path (user's tool); Logseq supports create + property/managed-block refresh."
    },
    {
      "id": "zotero-glean-compact-envelope",
      "summary": "Fixed --compact/--agent collapsing resource-envelope list items to {key}. compactListFields (internal/cli/helpers.go) applied a flat top-level allowlist, but Zotero items nest all real fields under data, so agent output for `items list` (and any envelope-shaped resource) reduced each item to {key}. compactListFields is now envelope-aware: when an item has a nested data object it keeps the top-level identifiers plus the data object compacted via compactObjectFields, and abstractNote/relations were added to that verbose-field blocklist.",
      "reason": "Discovered while validating vault sync on Zotero 10.0-beta.7: `items list --agent` returned only item keys, useless to agents, because the generic compaction allowlist assumes flat objects and Zotero (like any JSON:API-style API) uses a {key, data:{...}} envelope.",
      "files": [
        "internal/cli/helpers.go",
        "internal/cli/helpers_gaps_test.go"
      ],
      "validated_outcome": "TestCompactFieldsPreservesEnvelopeData asserts the envelope keeps key + nested title/itemType/DOI while stripping abstractNote/relations; the existing flat-object compaction test still passes; live `items list --agent` on Zotero 10-beta now returns the full useful item minus verbose fields. gofmt/build/vet and full `go test ./...` green.",
      "upstream_issue": "Systemic across printed CLIs whose API uses resource envelopes — candidate fix for cli-printing-press compactListFields (make it envelope-aware rather than flat-allowlist-only)."
    },
    {
      "id": "zotero-schema-drift-probe",
      "summary": "Added `schema drift`, a Zotero schema-currency probe (internal/cli/schema_drift.go) registered under the generated `schema` group. It fetches an order-independent fingerprint of the running Zotero's item types, item fields, and creator fields (and, under --deep, per-item-type field/creator-type validity), stores it as a baseline JSON file (~/.local/share/zotero-pp-cli/schema-baseline.json, not group-scoped because the schema is global to the install), and on later runs diffs the live schema against the baseline to report added/removed members. First run captures the baseline; --update re-baselines; --baseline overrides the path. Output is a pure function of the snapshots (no timestamps) so repeated runs are byte-identical. Added a `which` index entry for discoverability.",
      "reason": "User asked how to detect what a Zotero beta upgrade adds; regeneration/awc7 do not discover anything (endpoints are frozen at Web API v3), but new item types/fields are the realistic between-version delta and the data is already available from the live API.",
      "files": [
        "internal/cli/schema_drift.go",
        "internal/cli/schema_drift_test.go",
        "internal/cli/schema.go",
        "internal/cli/which.go"
      ],
      "validated_outcome": "Unit tests: first-run baseline capture, no-drift on identical schema, added/removed deltas (+preprint -book, +citationKey), and human output, all against a real client (client.BaseURL) pointed at an httptest schema surface. gofmt/build/vet and full `go test ./...` green. Live smoke on Zotero 10.0-beta.7: captured 40 item types / 123 item fields / 3 creator fields, no-drift on re-run, correct +item-types delta after a simulated upgrade, and --deep resolved per-type fields/creators for all 40 types. The probe strips the /users|groups/<id> library prefix because Zotero schema endpoints are global (/api/itemTypes).",
      "upstream_issue": "Discovered pre-existing bug: the generated `schema item-types|item-fields|item-type-fields|item-type-creator-types|creator-fields` commands 404 against the local API because they prepend the /users/<id> library prefix to global schema endpoints. Systemic for any printed CLI whose spec mixes library-scoped and global endpoints under one prefixed base_url — candidate fix for cli-printing-press (per-endpoint base-path/scope override). schema drift works around it locally by stripping the prefix for its own fetches."
    },
    {
      "id": "zotero-items-file",
      "summary": "Added `items file <itemKey>` (internal/cli/items_file.go), registered under the generated items group. Resolves an attachment's on-disk location via the local-API file endpoint /items/<key>/file/view/url: treats the key as an attachment directly, else resolves the item's PDF attachment via /items/<key>/children (reusing findPDFAttachmentKey). Prints the file:// URL decoded to a filesystem path, or a JSON envelope {item_key, attachment_key, url, path}. Complements `items open` (which launches the desktop app) by handing an agent/script the actual file to read.",
      "reason": "Coverage gap found while reviewing Zotero release notes/Local API docs: /items/<key>/file* (local-only attachment file access) was the one genuinely useful endpoint the CLI did not expose.",
      "files": [
        "internal/cli/items_file.go",
        "internal/cli/items_file_test.go",
        "internal/cli/items.go"
      ],
      "validated_outcome": "Unit tests (real client against httptest): direct attachment key, parent-item child-PDF resolution, and the no-attachment error. gofmt/build/vet and full `go test ./...` green. Live smoke on Zotero 10.0-beta: `items file 9UXV5R7L` resolved its child PDF (RLRDANQN) to /Users/ellis/Zotero/storage/RLRDANQN/...pdf (verified the file exists on disk); direct attachment key returns the same; percent-encoding is decoded."
    }
  ]
}
