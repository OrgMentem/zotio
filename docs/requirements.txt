# Documentation toolchain. Human-edited source of truth for the pinned SSG version;
# local docs installs use this file; CI installs the hash-pinned lock file.
#
# Zensical is pre-1.0 (alpha) and reads mkdocs.yml natively. Pin it hard and bump
# deliberately. On a version bump, edit docs/requirements.txt then regenerate
#   uv pip compile --universal --generate-hashes --python-version 3.14 docs/requirements.txt -o docs/requirements.lock.txt
zensical==0.0.57

# Fallback engine — same mkdocs.yml also builds with Material for MkDocs.
# Uncomment to build with `mkdocs build` instead of `zensical build`.
# mkdocs-material==9.6.9
