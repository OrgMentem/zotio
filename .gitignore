# Built binaries
/zotio
/zotio-mcp
/bin/

# Release staging / packaged MCP bundle
/build/
/dist/

# Harness-materialized skill store (symlinks into the dev skills dir)
/.agents/

# Vendored Printing Press dev skills (repo-local, refresh from upstream; not project source)
.claude/skills/
skills-lock.json
