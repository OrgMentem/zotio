linters:
  enable:
    - bodyclose
    - errcheck
    - errorlint
    - gosec
    - govet
    - ineffassign
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt
    - goimports

linters-settings:
  gosec:
    excludes:
      # web_api_key is a config-key *name*, not a credential value (false positive).
      - G101
